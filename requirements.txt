# ================================================================
# requirements.txt — KNUST Final Year Project — Group 4
# System Resource Optimizer
#
# Install with:  pip install -r requirements.txt
# ================================================================

# ── Core system monitoring ───────────────────────────────────────
psutil>=5.9.0

# ── Data / ML ────────────────────────────────────────────────────
# numpy is capped below 2.0: torch 2.2.x is built against the numpy 1.x
# C-API and fails to initialise ("_ARRAY_API not found") under numpy 2.
numpy>=1.24.0,<2.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ── Deep Learning (GRU model) ────────────────────────────────────
torch>=2.0.0,<2.3; platform_machine != 'arm64'
torchvision>=0.15.0,<0.18; platform_machine != 'arm64'

# ── ONNX inference ───────────────────────────────────────────────
onnx>=1.14.0
onnxruntime>=1.16.0; platform_machine != 'arm64'

# ── GUI ──────────────────────────────────────────────────────────
flet>=0.22.0
flet-desktop>=0.22.0

# ── Desktop notifications (optional — app runs fine without it) ──
plyer>=2.1.0
winotify>=1.1.0
